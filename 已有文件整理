类
class 类名(继承的类，没有填null){attr{类成员}def 类方法名(参数){定义}}
attr{声明 类成员以%开头 有默认值的 default %成员名 默认值}

函数
def 函数名(参数){定义}

数据类型
int 整型
float 浮点型
multi[精度] 多精度型
str 字符串
array 列表
hash 哈希
类名 实例

运算符
+ 加
- 减
* 乘
/ 除
** 平方
< 小于
> 大于
= 等于
!= 不等于
and 和
or 或
not 取反

逻辑结构
if(条件){执行代码}
elif(条件){执行代码}
else{执行代码}
while(条件){循环体}

数据类型子属性
array:
count() 计算元素的数量
add(元素,下标)
添加之后后面的元素下标依次增加
delete(元素,下标)
删除之后后面的元素下标依次减少
str:
add(元素,索引)
添加之后后面的元素索引依次增加
delete(元素,下标)
删除之后后面的元素索引依次减少
break(关键字) 切分字符串,以关键字为凭据
index(正则) 查询一个字符串在另一个字符串里的位置，没有返回null，有返回索引,不区分大小写
$index(正则) 查询一个字符串在另一个字符串里的位置，没有返回null，有返回索引，区分大小写
count() 计算字符串的长度

正则:
特殊字符
@任意一个字符 单独查询@需转意/@
#任意多个字符 单独查询#需转意/#

模块
subcmd{函数{函数体}函数{函数体}}

模板
template
 模板名{构造方法}

 calss   类定义
 default 构造初始值定义
 def     函数定义
 public  公共成员声明
 private 私有成员声明
 while   条件循环
 if      
 else
 elif

 sbcmd 
 import 


待确定问题：
1 bit 运算
2 语句分割
3 参数传递方式
4 是否允许指针
5 Boolean 规范
6 是否引入三目运算
7 内存开辟
8 函数签名（返回值形式）
9 是否引入命名空间（具体形式）